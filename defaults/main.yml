---
# Docker Engine configuration
# --------------------------------

# List of packages to install
docker__packages_name:
  - docker-ce

# The name of the systemd docker service
docker__service_name: docker

#docker__config_vars_file:

docker__host: "unix:///var/run/docker.sock"
#docker__storage_driver:

docker__storage_opt : {}

#docker__tlsverify:
#docker__tlscacert:
#docker__tlscert:
#docker__tlskey:
